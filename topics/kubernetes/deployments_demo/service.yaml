apiVersion: v1
kind: Service
metadata:
    name: namespace-demo-service
    namespace: dev
spec:
    selector:
        app: namespace-demo
    ports:
        - protocol: TCP
          port: 80
          targetPort: 5000